services:
  challenge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ctf-react2shell
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: unless-stopped